language: node_js
sudo: true
dist: trusty

node_js:
  - '6.9.1'

before_script:
  - npm install -g @angular/cli

before_install:
  - export CHROME_BIN=/usr/bin/google-chrome
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start